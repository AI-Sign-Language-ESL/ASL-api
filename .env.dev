# =============================================================================
# ENVIRONMENT
# =============================================================================
ENVIRONMENT=DEV

# =============================================================================
# DJANGO CORE SETTINGS
# =============================================================================
# For development, use a simple secret key (NEVER use this in production!)
SECRET_KEY=dev-secret-key-change-me-locally-if-you-want

# Hosts that Django will accept requests from
ALLOWED_HOSTS=localhost,127.0.0.1
PYTHONPATH=.
DJANGO_SETTINGS_MODULE=config.settings.dev

# =============================================================================
# DATABASE (PostgreSQL) - OPTIONAL IN DEV
# =============================================================================
# Note: Your settings.py uses SQLite in DEV mode, so these are optional
# If you want to test with PostgreSQL locally, install and configure it:
# 1. Install PostgreSQL on your machine
# 2. Create a database: createdb tafahom_dev
# 3. Uncomment and fill these values:



# Leave empty to use SQLite (default in your settings.py for DEV)
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=

# =============================================================================
# REDIS (for Django Channels) - OPTIONAL IN DEV
# =============================================================================
# Note: Your settings.py uses InMemoryChannelLayer in DEV mode
# If you want to test with Redis locally, install and configure it:
# 1. Install Redis: brew install redis (Mac) or apt install redis (Linux)
# 2. Start Redis: redis-server
# 3. Use these values:

REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# =============================================================================
# AI SERVICE CONFIGURATION
# =============================================================================
# Your AI/ML service endpoint (adjust to your actual service)
AI_BASE_URL=http://localhost:8001
AI_API_KEY=dev-ai-key-not-important
AI_TIMEOUT=30

# =============================================================================
# WEBSOCKET LIMITS
# =============================================================================
WS_MAX_MESSAGES_PER_SECOND=30
WS_MAX_CONNECTION_TIME=900

# =============================================================================
# CORS & CSRF (Frontend URLs)
# =============================================================================
# Add your frontend dev URLs here (React/Vue/Angular running on port 3000)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
GOOGLE_CLIENT_ID=954198035335-64fg39fu3vpksu4o44tki3c4bmec14jt.apps.googleusercontent.com

# =============================================================================
# SENTRY (Error Tracking) - OPTIONAL IN DEV
# =============================================================================
# Leave empty in development, or add Sentry DSN if you want error tracking
SENTRY_DSN=